{
  "name": "app-point-manager",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "nx serve",
    "build": "nx build",
    "test": "nx test",
    "install:all": "npm install && nx run-many --parallel --target=install --projects=web-point-manager,api-point-manager",
    "start:all": "nx run-many --parallel --target=serve --projects=web-point-manager,api-point-manager",
    "start:api-point-manager": "nx run-many --parallel --target=serve --projects=api-point-manager",
    "start:web-point-manager": "nx run-many --parallel --target=serve --projects=web-point-manager"
  },
  "devDependencies": {
    "@nx/angular": "^19.5.6",
    "@nxrocks/nx-spring-boot": "^10.0.2",
    "@types/bootstrap": "^5.2.10"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
